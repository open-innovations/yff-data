---
title: Search
url: /search/
nav_order:
  v2: 50
---

<section>
<p>Search the site for keywords, topics or geographic areas (e.g. constituency):</p>
{# 
  This uses the pagefind lume plugin https://lume.land/plugins/pagefind/ - loaded and configured in _config.js
  Styling overrides are in src/_includes/css/search.css
#}
<div id="search"></div>
</section>

<section class="boxed">
<p>Or browse the site topics:</p>

{{ comp.filter({ label: 'Type to filter', selector: 'ul.tag-cloud' }) | safe }}

{{ comp.tagCloud({ tags: search.pages("topic", "title") | data | getAttr('title'), live_links: true }) | safe }}
</section>
