---
title: Search
url: /search/
nav_order:
  v2: 50
---

<p>You can search the site by topic.</p>

{{ comp.filter({ label: 'Type to filter the topic list', selector: 'ul.tag-cloud' }) | safe }}

{{ comp.tagCloud({ tags: search.pages("topic", "title") | data | getAttr('title'), live_links: true }) | safe }}

<p>Alternatively use the search box to perform a free-text search</p>
{# 
  This uses the pagefind lume plugin https://lume.land/plugins/pagefind/ - loaded and configured in _config.js
  Styling overrides are in src/_includes/css/search.css
#}
<div id="search"></div>