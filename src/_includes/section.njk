{% extends 'layout.njk' %}

{% block main %}
  <div class="page-title">
	<div class="holder">
		<a href='{{ report }}'>Go back to full report</a>
		<h1>{{ title }}</h1>
	</div>
  </div>

  <div class="wrapper holder">
  <aside>
  {{ comp['tag-cloud']({ tags: tags }) | safe }}
  </aside>

  <section>
  {{ summary | md | safe }}
  </section>

  {% for block in blocks %}
    {{ comp.report.block(block) | safe }}
  {% endfor %}
  </div>
{% endblock %}